export interface ExtensionSharedInfo {
  reqUrl: string;
  reqReferrer: string;
  reqBodyCity?: string;
  reqBodyInventory?: string;
  reqBodyTrade?: string;
  worldId: string;
  sessionId: string;
}
