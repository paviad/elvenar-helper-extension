/** Pixels per tile at zoom 1 in the top-down view. */
export const GridSize = 15;

/** The playable city grid is GridMax x GridMax tiles. */
export const GridMax = 80;

/** Tiles of scratch space rendered around the grid, so blocks can be parked outside it. */
export const PaddingTiles = 20;

/** Fill opacity for a block, so the grid stays visible underneath. */
export const BlockOpacity = 0.8;
